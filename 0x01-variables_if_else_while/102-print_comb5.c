#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
*/
int main(void)
{
	int x;
	int y;
	int a;
	int b;
	int z:

	for (x = 0 ; x <= 9 ; x++)
	{
		for (y = 0 ; y <= 9 ; y++)
		{
			for (x = a ; a <= 9; a++)
			{
				if ( x == a)
				{
					(z = y + 1) 
				else 
				{
					(a == '0')
				for (; z <= 9; a++)
				{
						putchar(x + '0');
						putchar(y + '0');
						putchar(' ');
						putchar(z + '0');
						putchar(a + '0');
						if (!(x == 9 && y == 8 && z == 9 && a == 9))
						{
						putchar(',');
						putchar(' ');
						}
						}
					}
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
